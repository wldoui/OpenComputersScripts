-- ==================================================================
-- manual_api_notes.lua
--
-- Ручной механизм описания API (пункт 3 задания).
-- api_inspector.lua вызывает только методы без аргументов (getters).
-- Методы, которым нужны параметры (например setLevel(percent),
-- getCoords(x, y)), в скане покажутся с [ERROR] и текстом ошибки —
-- это нормально и означает "нужны аргументы", а не "метод не работает".
--
-- Сюда вручную дописывай, что ты выяснил, поигравшись с методом
-- через обычный OpenOS lua REPL, например:
--
--   local c = component.proxy(component.get("xxxxxxx"))
--   c.setCoords(100, 200)
--
-- Формат: components[UUID_или_type] = { методы... }
-- Это НЕ исполняемый драйвер, это просто база знаний для человека
-- и для последующих драйверов в /drivers/.
-- ==================================================================

local notes = {}

-- Пример заполнения (удали/замени на реальные данные после теста):
--
-- notes["ntm_space_something"] = {
--   getPosition = {
--     args = "()",
--     returns = "table {x, y, z} в блоковых координатах",
--     verified = false, -- поставь true когда реально проверишь в игре
--   },
--   setDestination = {
--     args = "(x:number, y:number, z:number)",
--     returns = "boolean, true если цель принята",
--     verified = false,
--   },
-- }

return notes
